---
name: project-vtree-newbase-vd-migration
description: Local VTree stack rebased onto new base (LSM-sampling ps12 + dummy VD); Training/3771 ported off the reorganized vector-distance API
metadata: 
  node_type: memory
  type: project
  originSessionId: f918823a-9e3b-4f5f-a2b3-6f30a6442e6e
---

**Branch `integrate-newbase`** (worktree `/tmp/vtree-newbase`) rebases our VTree stack onto the NEW gerrit base to unblock local dev while the real changes settle. Base chain: `94590af28d [3763 UI bun/Angular]` → `fb0c113ad2 [3702 LSM-sampling ps12 #20959]` → `dbd9b49e8a [DON'T MERGE dummy vector distance #21385]`. Then our: storage patch1/2/3 → 3760(=Calvin's ps20, ported) → 3771(ours, ported). Main branch `gerrit/storage-wrap-up-integrate` untouched.

**Why the migration:** the vector-distance function was intentionally reorganized in BOTH the dummy AND real 3676 PS36 (#20126): `VectorDistanceArrCalculation`→`VectorDistanceCalculation` (double[]-only), `dot`→`dotProduct`, DROPPED `normalizeL2` + `float[]`/`byte[]` quantized overloads, evaluator `VectorDistanceArrScalarEvaluator`(6 typed method-refs)→`VectorDistanceScalarEvaluator`(1 `ToDoubleBiFunction<double[],double[]>`), `*ArrDescriptor`→`*Descriptor`. Storage (3754) rebases CLEAN (uses its own hyracks `IVTreeDistanceFunction`/`VectorUtils`, no runtime-VD dep). Training(3760)+3771 heavily use the old runtime API → needed porting.

**Port done (option b: adapt Training, don't touch the distance patch):** built on Calvin's ps20 (Training+amendment coherent, keeps his bottom-up emission — avoids the ps19-Training-vs-amendment double-refactor mess). Ported files: `HierarchicalKMeansPlusPlusCentroidsOperatorDescriptor`, `VTreeBulkLoaderAndGroupingOperatorDescriptor`, `VectorDistanceFunctionFactory`, `ANNDistanceDescriptor`, `FunctionCollection`, `ErrorCode`, `en.properties`. Renames done via word-boundary perl (keeps `*FunctionDouble` class names, which now `implements IVTreeDistanceFunction`). `normalizeL2`/`l2Norm` relocated as private statics into HierarchicalKMeans (they were Training-added, safe = no quantization magic; old float[]/byte[] methods were just type-promoted double[] math). `ErrorCode` collision (both sides claimed 1246-1255, dup INVALID_FULL_SCAN_OPTION) resolved: keep new base geo 1246-1255, renumber vector codes to 1256-1264, drop dup; `en.properties` (3771 owns the vector messages) renumbered to match. Deleted obsolete `VectorDistanceArrCalculation.java`; took new base's `CosineDistanceDescriptor`.

**2026-07-01 — integrate-newbase is now the COMPLETE working branch.** This round's edits ([[project-vtree-distance-injection]]) + the previously-uncommitted newbase-3771 porting were folded into its commits via fixup+autosquash: G1→patch1, G2→patch2, G3→patch3, G4(provider/VDFF/staticstructure)→3760, G5(registry)+3 uncommitted 3771-porting files(VectorIndexAccessMethod id-remap, ANNDistanceDescriptor/CommonFunctionMapUtil cosmetic)→3771. One adaptation needed: static-structure descriptor's `VectorDistanceArrCalculation.euclidean` → `VectorDistanceCalculation.euclidean` (renamed on newbase). Stack now `[ps12]→[dummy VD]→patch1→patch2→patch3→3760→3771`, all folded. VERIFIED: builds green, 19/19 unit, integration Parts 3+4 all pass, 0 NPEs. `gerrit/storage-wrap-up-integrate` was REVERTED to pre-amend (old-base, `60ef46e7da`) — recovery tags: `amend-done`(old-base amended), `amend-distance-safety`(old-base pre-amend), `newbase-fold-safety`(newbase pre-fold). This-round per-group patches saved in `/tmp/thisround/`.

**Handoff amendment (for Calvin's 3760):** pristine ps20 = gerrit change **21159 ps20** (`refs/changes/59/21159/20`, tagged `gerrit-3760-ps20`). Amendment = `git diff gerrit-3760-ps20 <newbase-3760> -- <files in 3760>` = **7 files, 176 lines** = VD-interface porting (ErrorCode, BuiltinFunctions, HierarchicalKMeans, VTreeBulkLoader, VectorDistanceFunctionFactory) + this-round G4 (VTreeResourceFactoryProvider, VTreeStaticStructureCreator, VectorDistanceFunctionFactory toJson). Saved `/tmp/3760-amendment-for-calvin.patch`.

**DONE 2026-07-01 — `gerrit/storage-wrap-up-integrate` rebuilt to new-base split form:** `dummyVD → patch1/2/3 → 3760(=ps20 content) → amendment → 3771`. Built by taking integrate-newbase and SPLITTING its single 3760 commit (only 7 files differ from ps20, so deterministic): C_ps20 = 3760-tree with those 7 files reverted to pristine ps20 blobs (Change-Id I1264840, Calvin authorship preserved via `git commit -C`); amendment = restore the 7 files to newbase-3760 (Change-Id I5d1343, msg rewritten for VD-porting+injection); 3771 cherry-picked on top (Iaa502b). VERIFIED: tip tree == integrate-newbase tip (byte-identical, already tested green); C_ps20's 3760-file content == pristine ps20; amendment == the 7-file delta. Same handoff shape as before: C_ps20 alone doesn't compile (old-VD refs), amendment ports it, tip compiles. PUSHED 2026-07-01: origin force-pushed (`60ef46e7da→131804faaf`, Calvin cherry-picks amendment `3909f51046` onto his 3760 ps20); gerrit storage patch1/2/3 new patchsets (changes 21099/21100/21101) rebased onto dummyVD — needed `git push --no-thin` (thin-pack "missing blob" error otherwise); dummyVD(21385)/ps12(20959) left untouched in the relation chain. 3760/amendment/3771 NOT pushed to gerrit (3760 is Calvin's; amendment/3771 handed off via origin). Old gerrit-branch position saved as tag `amend-distance-safety` (60ef46e7da). Recovery tags: `amend-done`, `newbase-fold-safety`, `gerrit-3760-ps20`(=change 21159 ps20). integrate-newbase = same content, 3760+amendment combined into its single 3760 commit.

**BUILD-VERIFIED 2026-06-30:** `mvn clean package -DskipTests` on the whole asterixdb reactor = BUILD SUCCESS (all modules incl asterix-server, asterix-dashboard). One extra fix beyond the mechanical port was needed: `VectorIndexAccessMethod.java` (3771/asterix-algebra) referenced OLD BuiltinFunctions distance ids — mapped `EUCLIDEAN_DISTANCE_ARRAY`→`EUCLIDEAN_DISTANCE`, `EUCLIDEAN_SQ_DISTANCE_ARRAY`→`EUCLIDEAN_SQUARED_DISTANCE`, `COSINE_DISTANCE_ARRAY`→`COSINE_DISTANCE`, `DOT_DISTANCE_ARRAY`→`DOT_PRODUCT`. Build caveat: the `git-commit-id-plugin` fails in a git WORKTREE (`.git` is a file) — skip with `-Dmaven.gitcommitid.skip=true -Dgit.skip=true` (or build in a normal checkout). hyracks-fullstack installed to ~/.m2 with new-base artifacts, so the main branch's build is now stale — rebuild to switch back. This is a clean, compiling reference to hand Calvin for the 3760 VD-port. See [[project-gerrit-stack-handoff-crosspollination]].
