# Memory Index

- [Avoid em-dashes](feedback-avoid-em-dashes.md) — user prefers plain sentences over heavy "—" use in prose/artifacts/commits
- [VTree review deferred follow-ups](vtree-review-deferred-followups.md) — running backlog of code changes DEFERRED during 3754/3760/3771 comment addressing (compile-time ann_distance validation+B3, CRASH/failure-recovery for multi-step build, open-field reject, OSQ corrective-multiplier + renames, maxEntriesPerPage removal); update on every "follow-up" reply
- [VTree external integration suite](vtree-external-integration-suite.md) — live-cluster movie/glove harness at `integration/tests` (driver `run_tests.py`, lifecycle-managed); datasets outside repo at `~/Projects/asterixdb-schema-knn/integration/datasets`; distinct from SqlppExecutionTest golden runtimets
- [VTree CI verification workflow](vtree-ci-verification-workflow.md) — the 4-tier fast-to-slow flow (storage unit → runtime unit → golden runtimets → external integration); run all four before pushing a VTree change
- [VTree storage test scope for review fixes](feedback-vtree-storage-test-scope.md) — when fixing review comments, run ONLY vtree storage tests (install changed module, then offline vtree-test module); skip full -am/BTree suite until pre-push
- [Gerrit reply-draft threading](feedback-gerrit-reply-threading.md) — reply drafts must replicate the parent comment's exact anchor (same patch_set SHA + range + line + side), not just in_reply_to, or they orphan and the parent stays unresolved
- [VTree review fix: no provenance](vtree-review-fix-no-provenance.md) — don't add @AiProvenance on Gerrit review-comment fixes (they fold into existing attributed commits; upstream Apache diff stays clean)

- [VTree branch model](vtree-branch-model.md) — VTree/ANN project: Gerrit chain 3771/3760/3754→LSM, GitHub remote `vtree` (calvin-dani); integrated onto couchbase/master (`vtree-integ`) + cbas gate patch (cbas branch `vtree`) → VTree vector index works end-to-end on columnar
- [VTree Gerrit committer email](vtree-gerrit-committer-email.md) — Gerrit push as Le0shy rejects couchbase committer email; force-rebase committer to leoshy1005 before pushing (authors preserved: Calvin on 3760/spann)
- [VTree integration recall variance](vtree-integration-recall-variance.md) — glove_cosine ANN recall is non-deterministic (~47-56%) and sits on the 50% fail-threshold; a lone FAIL there is variance, re-run parts 1 2 before investigating
- [VTree ANN query conditions](vtree-ann-query-conditions.md) — when a vector query uses the index (ann_distance: constant vector + matching metric + no NULLS LAST) vs full scan (vector_distance always exact); min_probe_fraction 1.0 == brute force; ~/vector-queries/ ready queries
- [VTree 3760 test plan](vtree-3760-test-plan.md) — deferred unit tests for the 3760 review round; Milestone B leans on existing CI regression net (storage vtree suite / sqlpp vector goldens / integration recall) first
- [VTree review-fixes patchset mapping](vtree-review-fixes-patchset-mapping.md) — WIP checkpoint `c3921f52c6` bundles p1 review fixes + p2 seed-orphan fix + 3760/3771/docs; which file → which Gerrit patchset; next = tuple-format accessor refactor
- [Analytics project switching](analytics-project-switching.md) — routine to switch between CLUSTER BY and VTree: vtree=main superproject, clusterby-Gerrit=isolated ~/clusterby-gerrit-wt; switch by `cd`; `~/switch-analytics-project.sh` for the main-worktree flip
- [CLUSTER BY branch model](clusterby-branch-model.md) — clean `clusterby` vs derived `clusterby-dev`; in-flight Gerrit deps (vector distance, LSM sampling) + how to regenerate the integration
- [CLUSTER BY Phase 2 design](clusterby-phase2-design.md) — distributed k-means via logical-plan expansion; CENTROID aggregate + nearest_centroid fn; translator injection point; open decisions D1–D4
- [CLUSTER BY runtime dataflow](clusterby-runtime-dataflow.md) — two-phase tower→label model; full records never enter the k-means tower (only embeddings, teed via REPLICATE not re-scan); rows self-label via nearest_centroid(embedding, Cfinal); kmeansPP-exact mechanics + perf TODOs; plan-golden gotcha (-validate = SQL++ oracle, -syntax = real path)
- [CLUSTER BY vector materialization layers](clusterby-vector-materialization-layers.md) — 3 layers (scan/REPLICATE/operator-internal); Approach A changed only layer 3 (~9→1 internal, writer+drains), REPLICATE stays; A′ targets dropping REPLICATE; Lloyd WEIGH/LLOYD split + {count,sum}
- [CLUSTER BY exact-sampling desugar bench](clusterby-exact-sampling-desugar-bench.md) — prototyped paper-exact Bernoulli init as desugar; CONFOUNDED by pool-as-list frame overhead (both forms slow); exact sampling at scale belongs in the OPERATOR not desugar
- [CLUSTER BY test label-invariance](clusterby-tests-label-invariance.md) — k-means cluster_id labels are arbitrary; assert the partition/counts, not exact labels
- [CLUSTER BY commit style](clusterby-commit-message-style.md) — no Co-Authored-By: Claude on clusterby Gerrit commits (AiProvenance covers it)
- [CLUSTER BY docs workflow](clusterby-docs-workflow.md) — design/status docs live in separate `~/clusterby-docs` repo, symlinked as `doc-clusterby`, git-ignored so they never reach Gerrit
- [Build main worktree](build-main-worktree.md) — rebuild via `mvn clean package -DskipTests` inside the inner `asterixdb/asterixdb/` reactor, not a combined install
- [Ask before implementing](ask-before-implementing.md) — present design, get explicit permission BEFORE editing product code; also ask before every Gerrit push
- [Don't mutate cluster state](dont-mutate-cluster-state.md) — never drop/create indexes/datasets or load data on the user's running cluster yourself; READ only, hand DDL to the user to run
- [VTree gate Gerrit push](vtree-gate-gerrit-push.md) — cbas VTree gate patch → Couchbase Gerrit (user hongyushi-creator, cbas-core, topic vtree-gate); same-topic asterixdb change gets CI to build without merging
- [VTree cbas-gate manifest bridge](vtree-cbas-gate-manifest-bridge.md) — get cbas VTREE gate (MB-72975, change 249142) to compile in Couchbase CV via a manifest override pointing asterixdb at ASF change ref 21287/26 (topic hongyu/vtree); no Couchbase asterixdb project exists
- [Hold storage-patch push](feedback-hold-storage-patch-push.md) — prepare the squash/round push but STOP before pushing storage patches; user reviews + adds first, confirms explicitly
- [No cherry-pick annotation](feedback-no-cherry-pick-annotation.md) — never leave "(cherry picked from commit …)" lines in VTree/asterix commit messages; avoid `git cherry-pick -x`
- [VTree ASF commit-message workflow](vtree-asf-commit-message-workflow.md) — mentor's required commit format for ASF pushes (template, flags, Ext-ref: MB-72975, strip cherry-pick, committer=leoshy); filter-branch reword recipe + zsh `${VAR}:` brace gotcha
- [VTree infra access cheat-sheet](vtree-infra-access-cheatsheet.md) — how to reach BOTH Gerrits (ASF Le0shy / Couchbase hongyushi-creator) and ALL THREE Jenkins (asf / analytics / server — each different token), push workflows, and current change/branch state
- [VTree ASF CI retrigger](vtree-asf-ci-retrigger.md) — re-running a -1 ASF asterix CV build: use the build UI **Retrigger** link (self-service, single job, GerritUserCause, checks out + votes back); Jenkins "Rebuild" is BROKEN (strips GERRIT_REFSPEC → posts another -1); GlobalVirtualBufferCacheTest is known-flaky (not VTree)
- [Artifact SQL WAF reset](artifact-sql-waf-reset.md) — user's network TCP-resets claude.ai artifacts containing literal SELECT..FROM ("connection closed"); mask SQL keywords + reveal at runtime
- [CLUSTER BY Route B systolic loop](clusterby-routeB-systolic-loop.md) — M3 BUILT & validated multi-NC (2-NC harness green, HEAD 4ab39a9866); 5-op systolic sub-graph for OVERSAMPLE_LOOP, adaptive nNC gate; remaining = M5 movie-scale parity/timing + single-NC regression
